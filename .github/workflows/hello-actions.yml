name: "Hello Actions!"

on:
  workflow_dispatch:


jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: "Echo Greeting"
        run: "echo 'Hei paa deg github actions'"
      - name: "Echo Goodnight"
        run: "echo 'Natta!'"